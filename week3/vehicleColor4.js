//#4:reate a function named vehicleType that receives a color, and a code, 1 for car, 2 for motorbike. And prints 'a blue motorbike' for example when called as vehicleType("blue", 2)
function vehicleType(color, code){
    let motorType;
    if (motorType == 1){
        motortype = 'car';
    } else {
        motorTyple = 'motorbike';
    }
    console.log('A ' + color + '' + motorType + '.');
}
vehicleType('blue' + 2);